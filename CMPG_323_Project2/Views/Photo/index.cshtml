@model IEnumerable<CMPG_323_Project2.Models.Photo>

@{
    ViewData["Title"] = "index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>index</h1>

<p>
    <a asp-controller="PhotoMetaData" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PhotoUrl)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PhotoUrl)
            </td>
            <td>
                <a asp-controller="MetaData" asp-action="Edit" asp-route-id="@item.PhotoId">Edit</a> |
                @*&@item.PhotoUrl*@

                <a asp-controller="PhotoMetaData" asp-action="Details" asp-route-id="@item.PhotoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PhotoId">Delete</a>|
               
            </td>
        </tr>
}
    </tbody>
</table>
